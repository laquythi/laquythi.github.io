package communityuni.com.model;

public interface ILamViec {
    // toan bo method trong interface mac dinh la public(ko the private,protected) va abstract
    public void complexity();
    public void difficulty();
}
